{
  "title": "Airbnb Listings Scraper",
  "description": "Extract Airbnb listings and property details via API using a search URL.",
  "type": "object",
  "schemaVersion": 1,
  "properties": {
    "urls": {
      "title": "Airbnb Search URLs",
      "type": "array",
      "description": "One or more Airbnb /s/.../homes URLs. The actor will iterate all URLs until results_wanted is reached or sources are exhausted.",
      "editor": "stringList",
      "prefill": [
        "https://www.airbnb.com/s/London--United-Kingdom/homes?checkin=2026-05-13&checkout=2026-05-14&adults=1"
      ],
      "items": {
        "type": "string"
      }
    },
    "results_wanted": {
      "title": "Results wanted",
      "type": "integer",
      "description": "Maximum number of listings to save.",
      "minimum": 1,
      "default": 20,
      "prefill": 20,
      "editor": "number"
    },
    "proxyConfiguration": {
      "title": "Proxy configuration",
      "type": "object",
      "description": "Optional Apify Proxy settings. Enable residential routing manually when needed.",
      "editor": "proxy",
      "default": {
        "useApifyProxy": false
      }
    }
  },
  "required": [],
  "additionalProperties": false
}
